# Per-device QEMU overlay, applied last by `scripts/qemu.sh`: the board and the
# display panel this device carries.

packages:
  board: !include board-d6-r6.yaml
  panel: !include panel-jxd-display.yaml
